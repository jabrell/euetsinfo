# Data folder

In this folder all data are stored. The structure of the folder follows the
structure of the processing process:

1. **source** The folder to store source data. This is where downloads etc. are
eventually stored
2. **normalized** After downloading the data, the column names and some other
basic normalizations are applied. The resulting data are store in this folder.